% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{dgamma}
\alias{dgamma}
\title{Gamma distribution probability density function}
\usage{
dgamma(x, shape, rate = NULL, scale = NULL, log = FALSE)
}
\arguments{
\item{x}{A numeric (doubles) vector of positive values}

\item{shape}{The shape parameter (α > 0)}

\item{rate}{The rate parameter (β > 0). Exactly one of \code{rate} or
\code{scale} should be provided.}

\item{scale}{The scale parameter (θ = 1/β > 0). Exactly one of
\code{rate} or \code{scale} should be provided.}

\item{log}{Logical; if TRUE, return the log-density (default: FALSE)}
}
\value{
The gamma PDF value f(x | α, β), or ln(f) if \code{log = TRUE}.
}
\description{
Computes the PDF of the Gamma(shape, rate) distribution at x,
using log-space arithmetic with a Lanczos approximation for ln Γ(α)
adapted from the Boost.Math C++ library's lanczos13m53 parameter set.

Supports both rate and scale parameterisations, matching R's
\code{\link[stats]{dgamma}} interface. If neither \code{rate} nor \code{scale}
is provided, defaults to rate = 1.

f(x | α, β) = β^α / Γ(α) · x^(α-1) · e^(-βx)

Computed as:
ln f = α·ln(β) - ln Γ(α) + (α-1)·ln(x) - β·x
}
\examples{
# Exponential distribution (shape = 1)
statz::dgamma(1, shape = 1, rate = 2)

# Compare with R's dgamma
statz::dgamma(2, shape = 3, rate = 1)
stats::dgamma(2, shape = 3, rate = 1)

# Scale parameterisation
statz::dgamma(2, shape = 3, scale = 2)
}
