% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_models.R
\name{z_lm}
\alias{z_lm}
\title{Fit a linear model via Rust backends}
\usage{
z_lm(formula, data, engine = c("naive", "qr", "svd"))
}
\arguments{
\item{formula}{An object ob class "formula" (or one that can be coerced to that class).}

\item{data}{A data frame, tibble, or environment containing the variables in the models.}

\item{engine}{A character string specifying the computational backend.
Currently supports "naive" (Cholesky factorisation of normal equation).}
}
\value{
A list of class \code{z_lm} containing coefficients, standard errors,
fitted values, residuals, degrees of freedom, and the residual standard error.
}
\description{
Fits an ordinary least squares (OLS) regression using compiled Rust linear
algebra engines. Designed as a pedagogical alternative to \code{stats::lm()}.
}
\examples{
# Fit a basic model
fit <- z_lm(mpg ~ wt + cyl, data = mtcars)
}
