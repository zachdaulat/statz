% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{z_pgamma}
\alias{z_pgamma}
\title{Gamma cumulative distribution function}
\usage{
z_pgamma(x, shape, rate = NULL, scale = NULL)
}
\arguments{
\item{x}{A positive numeric value (quantile)}

\item{shape}{The shape parameter (α > 0)}

\item{rate}{The rate parameter (β > 0). Exactly one of \code{rate} or
\code{scale} should be provided.}

\item{scale}{The scale parameter (θ = 1/β > 0). Exactly one of
\code{rate} or \code{scale} should be provided.}
}
\value{
Cumulative probability P(X <= x)
}
\description{
Computes P(X <= x) for X ~ Gamma(shape, rate), using a Taylor series
for the lower regularised incomplete gamma function when x is small
relative to shape, and Legendre's continued fraction (via the modified
Lentz algorithm) for the upper complement when x is large.
}
\examples{
z_pgamma(2, shape = 3, rate = 1)
pgamma(2, shape = 3, rate = 1)

z_pgamma(1, shape = 1, rate = 1)   # Exponential: 1 - exp(-1)
}
