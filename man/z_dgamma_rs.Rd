% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{z_dgamma_rs}
\alias{z_dgamma_rs}
\title{Compute the gamma distribution probability density function.}
\usage{
z_dgamma_rs(x, shape, rate, log)
}
\arguments{
\item{x}{A positive numeric value}

\item{shape}{The shape parameter (α > 0)}

\item{rate}{The rate parameter (β > 0)}

\item{log}{Logical; if TRUE, return the log-density}
}
\value{
The gamma PDF value at x, or ln(PDF) if log = TRUE
}
\description{
Evaluates the PDF of the Gamma(shape, rate) distribution at x using
log-space arithmetic to avoid overflow:
}
\details{
f(x | α, β) = β^α / Γ(α) · x^(α-1) · e^(-βx)

Computed as:
ln f = α·ln(β) - ln Γ(α) + (α-1)·ln(x) - β·x

The log-gamma term is evaluated via \code{z_lgamma()} (Boost adaptation).
}
\section{Arguments}{
\itemize{
\item \code{x}     - A positive value at which to evaluate the density (x > 0)
\item \code{shape} - The shape parameter α > 0
\item \code{rate}  - The rate parameter β > 0 (inverse of scale)
\item \code{log}   - If true, return ln f(x) instead of f(x)
}
}

\section{Returns}{
The probability density f(x | α, β), or its natural log if \code{log = true}.
}

