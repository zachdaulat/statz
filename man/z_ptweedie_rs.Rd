% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{z_ptweedie_rs}
\alias{z_ptweedie_rs}
\title{Computing the Tweedie CDF (compound Poisson-Gamma)
Internal Rust engine for the Tweedie cumulative distribution.
Assumes all inputs (y, mu, phi, power) have been pre-validated by the R wrapper.
\itemize{
\item \code{y < 0} is not handled here (should be blocked by R).
\item Automatically routes exact zeros to point-mass fast paths.
\item Uses the Dunn & Smyth (2005) series expansion for y > 0.
\item Supports \code{lower_tail} and \code{log_p} evaluation.
}}
\usage{
z_ptweedie_rs(y, mu, phi, power, lower_tail, log_p)
}
\description{
Computing the Tweedie CDF (compound Poisson-Gamma)
Internal Rust engine for the Tweedie cumulative distribution.
Assumes all inputs (y, mu, phi, power) have been pre-validated by the R wrapper.
\itemize{
\item \code{y < 0} is not handled here (should be blocked by R).
\item Automatically routes exact zeros to point-mass fast paths.
\item Uses the Dunn & Smyth (2005) series expansion for y > 0.
\item Supports \code{lower_tail} and \code{log_p} evaluation.
}
}
