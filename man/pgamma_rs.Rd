% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{pgamma_rs}
\alias{pgamma_rs}
\title{Internal Gamma CDF Engine}
\usage{
pgamma_rs(x, shape, rate, lower_tail, log_p)
}
\arguments{
\item{x}{A positive numeric value (quantile)}

\item{shape}{The shape parameter (α > 0)}

\item{rate}{The rate parameter (β > 0)}
}
\value{
The cumulative probability P(X ≤ x | α, β)
}
\description{
Compute the gamma CDF: P(X <= x) for X ~ Gamma(shape, rate).

Dispatches to \code{lower_gamma_series} (Taylor series for P) when the
scaled argument z = rate · x is small relative to shape, and to
\code{upper_gamma_cf} (Legendre continued fraction for Q) otherwise.
The crossover at z = shape + 1 ensures that the directly computed
quantity is always the smaller of P and Q, avoiding precision loss
from subtraction near 1.
}
\keyword{internal}
