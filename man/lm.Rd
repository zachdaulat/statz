% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_models.R
\name{lm}
\alias{lm}
\title{Fit a linear model via Rust}
\usage{
lm(formula, data, engine = c("cholesky", "qr", "svd"))
}
\arguments{
\item{formula}{An object of class "formula" (or one that can be coerced to that class).}

\item{data}{A data frame, tibble, or environment containing the model's variables.}

\item{engine}{A character string specifying the computational backend.
Currently supports "cholesky" (fastest, requires full rank) or "qr" (numerically stable).}
}
\value{
A list of class \code{statz_lm} containing coefficients, standard errors,
fitted values, residuals, degrees of freedom, and the residual standard error.
}
\description{
Fits an ordinary least squares (OLS) regression using compiled Rust linear
algebra engines. Designed as a pedagogical alternative to \code{stats::lm()}.
}
\examples{
# Fit a basic model
fit <- lm(mpg ~ wt + cyl, data = mtcars)
}
